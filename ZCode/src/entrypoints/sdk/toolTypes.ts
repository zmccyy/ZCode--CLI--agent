/**
 * @generated — Stub for Node.js compatibility.
 * Tool types re-exported by agentSdkTypes.ts.
 * All marked @internal until SDK API stabilizes.
 */

import type { CallToolResult, ToolAnnotations } from '@modelcontextprotocol/sdk/types.js'

export type SdkToolOptions = {
  annotations?: ToolAnnotations
  searchHint?: string
  alwaysLoad?: boolean
}

export type { CallToolResult, ToolAnnotations }
