# T2.2 REPL 启动链路卡死诊断

## 状态

**未解决** — `bun src/entrypoints/cli.tsx --bare -p "hello"` 静默卡死（零输出），15 秒超时。

## 已排查的启动链路（全部通过）

```
cli.tsx:main()
  → import(earlyInput.js) ✓
  → import(main.js)      ✓
main.tsx:main()
  → eagerLoadSettings()  ✓
  → run()
    → preAction hook
      → ensureMdmSettingsLoaded/ensureKeychainPrefetchCompleted ✓
      → init()            ✓ (enableConfigs, safeEnvVars, mTLS, proxy, setShellIfWindows 全部通过)
      → getCommandName()  ✓
      → import(sinks.js)  ✓
      → runMigrations()   ✓
      → loadRemoteManagedSettings/loadPolicyLimits ✓
    → action handler
      → prompt/kairos/options destructure ✓
      → teammate/sdk/teleport  ✓
      → session/file-download  ✓
      → initializeToolPermissionContext ✓
```

## 卡死点

**位置**: `main.tsx` 第 1937 行

```javascript
const { setup } = await import('./setup.js');  // ← 卡死在这里
```

`setup.ts` 模块的顶层副作用（top-level side effects）或同步初始化代码在模块加载时阻塞。

## 下一步

1. 阅读 `src/setup.ts`，检查顶层 import 链和模块级别副作用
2. 在 `setup.ts` 顶层添加 stderr 诊断，定位具体卡死的 import 或函数
3. 重点怀疑：Windows 上不兼容的 fs/socket/child_process 操作
